{
  "name": "nimiq-settlement",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Shared Nimiq settlement core: RPC block-scan client + payment matcher/provider behind one NimiqClientLike seam. Extracted from nimiq-pos (proven live on mainnet).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "bun tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
